# Railway Environment Variables
# Copy these to Railway Dashboard > Variables

# ===========================================
# Database (Railway PostgreSQL - Auto-configured)
# ===========================================
# Railway automatically sets DATABASE_URL when you add PostgreSQL
# DATABASE_URL is auto-injected by Railway

# For Prisma direct connection (same as DATABASE_URL on Railway)
# DIRECT_URL is auto-injected by Railway

# ===========================================
# NextAuth.js Authentication
# ===========================================
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-min-32-chars

# Your domain (update after Railway deployment)
NEXTAUTH_URL=https://your-app.up.railway.app

# ===========================================
# AI - Qwen3 via Together.ai
# ===========================================
# Get free API key: https://api.together.xyz/
TOGETHER_API_KEY=your-together-api-key

# Model options:
# - Qwen/Qwen2.5-72B-Instruct (best quality, slower)
# - Qwen/Qwen2.5-7B-Instruct (faster, cheaper)
# - Qwen/Qwen2.5-Coder-32B-Instruct (for code)
QWEN_MODEL=Qwen/Qwen2.5-72B-Instruct

# AI Configuration
AI_TIMEOUT=60000
AI_MAX_RETRIES=3

# ===========================================
# Domain (after custom domain setup)
# ===========================================
# NEXTAUTH_URL=https://yourdomain.com
